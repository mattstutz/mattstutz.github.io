# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Matthias Stutz
email: info.matthias@stutz.slmail.me
description: >- # this means to ignore newlines until "baseurl:"
  Write an awesome description for your new site here. You can edit this
  line in _config.yml. It will appear in your document head meta (for
  Google search results) and in your feed.xml site description.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://mattstutz.github.io" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: jekyllrb
github_username:  jekyll

# Build settings
theme: "minimal-mistakes-jekyll"
minimal_mistakes_skin: "default"
plugins:
  - jekyll-paginate
  - jekyll-sitemap
  - jekyll-gist
  - jekyll-feed

# Standard-Einstellungen für alle Inhalte erzwingen
defaults:
  # Für alle normalen Seiten (z.B. portfolio.md, about.md)
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "single"
      author_profile: true
      # sidebar:
        # nav: "main" # Blendet auf allen Seiten das Hauptmenü in der Sidebar ein
  # Für alle Blog-Beiträge
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "single"
      author_profile: true
      read_time: true
      show_date: true


# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
# Feature Rows für das Portfolio
projekt_reihe:
  - image_path: /assets/images/projekt1.jpg
    alt: "Projekt 1 Screenshot"
    title: "Web-Anwendung Alpha"
    excerpt: "Eine moderne Webanwendung gebaut mit React und Ruby on Rails."
    url: "https://github.com/deinname/projekt1"
    btn_label: "Mehr erfahren"
    btn_class: "btn--primary"

  - image_path: /assets/images/projekt2.jpg
    alt: "Projekt 2 Screenshot"
    title: "Mobile App Beta"
    excerpt: "Cross-Platform Mobile App für Produktivität."
    url: "/portfolio/projekt-beta/"
    btn_label: "Details"
    btn_class: "btn--success"

sass:
    style: compressed
    quiet_deps: true

collections:
  portfolio:
    output: true
    permalink: /portfolio/:path/

main_navigation: main